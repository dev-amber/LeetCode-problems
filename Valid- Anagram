class solution{
public:
 bool isAnagram(string s, string t){
if(s.length() != t.length()
return false;
}
std::string sorted_s=s;
std::string sorted_t= t;
std:: sort(sorted_s.begin(),sorted_s.end());
std:: sort(sorted_t.begin(),sorted_t.end());
return sorted)_s==sorted_t;
}
