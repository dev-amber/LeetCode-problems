class Solution {
public:
    int findMaxConsecutiveOnes(vector<int>& nums) {
int count=0;
int maxCount=0;
for(int nums:num){
if(num==1){
count++; 
}else{
maxCount=max(maxCount,count);
Count=0;
}
}
maxCount=max(maxCount,count);
return maxCount;
}
};
