class solution{
public:
 int passThePillow(int n, int time) {
int cycle_lenght=2 * (n-1);
int effective_time=time % cycle_length;

if(effective+time <(n-1)){
return 1 + effective_time;
}else
return n-( effective_time -(n-1));
}
}
};
